Enforce a reasonable password policy.
Rate-limit registration.
Rate-limit login much more aggressively.
Implement email verification.
Implement password reset.
Consider account lockout/throttling for repeated failed logins.
AWS Cognito? check it
dead-letter queue (for async events. Send error there?)

Compress-Archive -Path lambda.js,package.json,package-lock.json,src,node_modules -DestinationPath lambda-deployment.zip -Force

errors must be visible in cloudwatch but now in response.
