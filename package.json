{
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.1116.0",
    "@aws-sdk/client-ses": "^3.1113.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0",
    "pg": "^8.22.0",
    "serverless-http": "^4.0.0",
    "validator": "^13.15.35"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test:lambda": "node -e \"import('./lambda.js').then(async ({ handler }) => console.log(await handler({ requestContext: { http: { method: 'GET', path: '/' } }, headers: {}, body: null, isBase64Encoded: false }, {})))\""
  }
}
